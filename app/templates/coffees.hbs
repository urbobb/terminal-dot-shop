<div class="container mx-auto px-4 py-12 bg-stone-950 text-gray-200">
  <h1
    class="text-6xl font-extrabold text-center text-white mb-8 drop-shadow"
  >Explore Our Exquisite Coffee Selection</h1>
  <p
    class="text-xl text-stone-400 mb-12 text-center max-w-2xl mx-auto leading-relaxed"
  >Welcome to our curated collection of the finest coffees. Each blend is
    carefully selected for its unique flavor profile and quality.</p>

  {{#each this.loadCoffees as |coffee|}}
    <div
      class="bg-gray-800 rounded-2xl shadow-xl mb-12 p-10 border border-gray-700 hover:shadow-2xl transition duration-500 ease-in-out transform hover:-translate-y-1"
    >
      <h2
        class="text-4xl font-bold text-gray-100 mb-6 border-b-2 pb-4 border-yellow-500"
      >{{coffee.name}}</h2>
      <p
        class="text-gray-300 mb-8 leading-relaxed text-lg"
      >{{coffee.description}}</p>

      <h3 class="text-2xl font-semibold text-gray-200 mb-5">Available Variants:</h3>
      <ul class="space-y-4">
        {{#each coffee.variants as |variant|}}
          <li
            class="flex justify-between items-center bg-gray-700 p-4 rounded-lg shadow-sm border border-gray-600"
          >
            <span
              class="text-xl font-medium text-gray-200"
            >{{variant.name}}</span>
            <span
              class="font-mono text-xl text-green-400 font-bold"
            >${{variant.price}}</span>
          </li>
        {{/each}}
      </ul>
    </div>
  {{/each}}

</div>